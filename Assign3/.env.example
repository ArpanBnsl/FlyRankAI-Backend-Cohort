POSTGRES_PASSWORD=replace-with-a-local-password
POSTGRES_DB=tasks
DATABASE_URL=postgresql://postgres:replace-with-a-local-password@localhost:5432/tasks
DATABASE_URL_DOCKER=postgresql://postgres:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
